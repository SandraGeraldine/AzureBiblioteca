<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <system.webServer>
    <!-- Configuración para Azure App Service -->
    <defaultDocument enabled="true">
      <files>
        <clear />
        <add value="public/index.php" />
        <add value="index.php" />
      </files>
    </defaultDocument>
    
    <!-- Rewrite rules para URLs limpias -->
    <rewrite>
      <rules>
        <!-- Redirigir raíz a public/index.php -->
        <rule name="Root to public" stopProcessing="true">
          <match url="^$" />
          <action type="Rewrite" url="public/index.php" />
        </rule>
        
        <!-- Redirigir a la carpeta public si no es un archivo existente -->
        <rule name="Redirect to public" stopProcessing="true">
          <match url="^(?!public/)(.*)$" />
          <conditions logicalGrouping="MatchAll">
            <add input="{REQUEST_FILENAME}" matchType="IsFile" negate="true" />
            <add input="{REQUEST_FILENAME}" matchType="IsDirectory" negate="true" />
            <add input="{REQUEST_URI}" pattern="^/(setup|test_connection|config)\.php$" negate="true" />
          </conditions>
          <action type="Rewrite" url="public/{R:1}" />
        </rule>
      </rules>
    </rewrite>
    
    <!-- Configuración de errores detallados -->
    <httpErrors errorMode="Detailed" />
    
    <!-- Habilitar PHP -->
    <handlers>
      <remove name="PHP_via_FastCGI" />
      <add name="PHP_via_FastCGI" path="*.php" verb="*" modules="FastCgiModule" scriptProcessor="D:\Program Files\PHP\v8.2\php-cgi.exe" resourceType="Either" requireAccess="Script" />
    </handlers>
  </system.webServer>
</configuration>
